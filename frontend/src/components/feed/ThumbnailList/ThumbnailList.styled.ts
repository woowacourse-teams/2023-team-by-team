import { styled } from 'styled-components';

export const Container = styled.ul`
  display: flex;
  overflow-x: auto;
  overflow-y: hidden;
  flex-direction: row;
  flex-shrink: 0;
  column-gap: 12px;

  width: 100%;
  height: 116px;
  padding-bottom: 20px;
`;
